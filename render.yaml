services:
  - type: web
    name: your-service-name
    env: node
    port: 10000  # This tells Render which port to expect
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
